# mosquitto.conf

# Listen on all interfaces (for remote clients like your ESP32)
listener 1883 0.0.0.0

# Allow unauthenticated access (good for prototyping)
allow_anonymous true

# Optional: log to stdout
log_dest stdout

